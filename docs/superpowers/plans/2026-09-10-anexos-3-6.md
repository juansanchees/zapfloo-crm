# Correções dos anexos 3–6 — Implementation Plan

**Goal:** corrigir os quatro defeitos confirmados da auditoria, sem PR, main ou deploy.
**Architecture:** opções locais do cliente do Copiloto; recuperação explícita do vínculo preparatório; registro e varredura de pontos de IA; validação coerente do tamanho do prompt no salvamento e preparação.
**Tech Stack:** Next 16, React 19, Node 22, pnpm via corepack, Vitest, Postgres 15/Supabase.
**Spec:** pedido do dono nesta tarefa, anexos 3–6 (10/set/2026).

## Restrições

- Base: `origin/codex/correcoes-producao` @ f920dac5e. Worktree próprio `correcoes-anexos`.
- Não editar `.codex/config.toml`, `.env*`, migrations aplicadas ou outros worktrees.
- 0228 para recuperação; 0229 para limites; 0231 reservada ao hardening.
- Todo schema: migration + apêndice idempotente do baseline + MANIFEST.
- Teste vermelho antes do código, verde depois, sabotagem e restauração comprovadas.
- Sem chamadas pagas de IA ou envio real de WhatsApp nos testes.

## Entregas independentes

- [x] Anexo 3: `CopilotPage.tsx`, teste do componente usando cliente real: resposta após 12s aparece uma vez; timeout e falhas não repetem POST custoso. Reduzir timeout deliberadamente e comprovar vermelho. Fragmento próprio.
- [x] Anexo 4: RPC `fn_recuperar_preparacao_onboarding` com admin/tenant, CAS de revisão/versão, apenas agente arquivado ou vínculo removido. Não reativa nem modifica agente existente. Limpa ponteiros/snapshot/ensaio obsoletos e audita. Action e botão de recuperação no painel existente; nome ocupado continua protegido e a tela orienta a alterá-lo. Testes: arquivar → recuperar → salvar novo nome → preparar; apagar → recuperar → preparar; bloqueios cross-tenant, ativo, publicado, revisão antiga e privilégios. Sabotar a limpeza e comprovar vermelho.
- [x] Anexo 5: registrar `onboarding_rehearsal` e detectar chamadas com ambas as aspas. Inserir ponto não registrado entre aspas duplas, rodar o gate real, comprovar vermelho e remover somente a sonda própria.
- [x] Anexo 6: validação do prompt montado no salvamento, inclusive objetivo, mantendo 20000 como teto. Provar fronteira exata e +1, Unicode, templates e segmentos. Rascunhos antigos permanecem recuperáveis, nunca truncados silenciosamente. Sabotar o limite e comprovar vermelho. Fragmento próprio.

## Integração e provas

- [x] Registrar limite da medição inicial: não houve baseline unitário isolado; a corrida já coletou o novo teste vermelho do Copiloto. Não alegar base verde.
- [x] Registrar Living System Checklist com consumidores, telas, logs e laço de retorno concretos; atualizar mapas e mapa de jornadas.
- [x] `corepack pnpm gov:verify`, rodapé completo e exit code.
- [x] `corepack pnpm test:db`, baseline INSTALL/UPDATE e RLS; árvore estática durante a corrida.
- [x] Prova de navegador para os caminhos visíveis; separar mocks de rede da integração real de banco.
- [x] Revisar diff, salvar commits por assunto e push da branch. Sem PR/deploy.
- [x] Relatório: concluído/testado, pendente, bloqueado, e o que não foi medido (`docs/testing/anexos-3-6.md`).
